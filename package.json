{
  "name": "task-tracker",
  "version": "1.0.0",
  "description": "Full-stack task tracker with Cloudflare Workers + D1",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "build:client": "cd client && npm install && npm run build",
    "deploy:all": "npm run build:client && wrangler deploy"
  },
  "dependencies": {
    "itty-router": "^4.0.20"
  },
  "devDependencies": {
    "wrangler": "^3.26.0"
  }
}
